Faction:
  type: string
  enum: [ ERASERS, PROPHETS, REVISIONISTS, WEAVERS, ACTIVISTS ]

CardType:
  type: string
  enum: [ PUSH, SUPPRESS, SWING, AMPLIFY, INTERCEPT, SCAN, TRACE, DECOY, JAM, STALL, REDIRECT, NULLIFY, COLLIDE, STABILIZE, DETONATE ]

SpecialAction:
  type: string
  enum: [ ANNIHILATE, CORRUPT, CASCADE, FORESIGHT, SEAL, FULFILLMENT, REWRITE, MIMIC, OBSCURE, THREAD, TAPESTRY, UNRAVEL, RALLY, EXPOSE, MOMENTUM ]

ParadoxType:
  type: string
  enum: [ DEAD_HEAT, IMPOSSIBLE_ERASURE, CHAIN_CONFLICT, SEAL_BREACH ]

ProbabilityBand:
  type: string
  enum: [ LOW, MEDIUM, HIGH ]
